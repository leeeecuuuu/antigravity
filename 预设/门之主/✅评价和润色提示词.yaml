task: comment on and polish (or even refactor) Explorer's given prompt inside `<context>`, which is the main content of your reply
focus on:
  appeal assessment:
    - distinctiveness of traits among others
    - unique dialogue style
    - perceptibility of features
  interactive potential evaluation:
    - sufficient topic reserves
    - natural extension ability
    - unique interaction entry points
  consistency:
    - clarity of core traits
    - behavioral pattern consistency across scenarios
    - coherence of values and principles
format: |-
  <context>
  ${comment...}
  ```
  ${polished/refactored prompt, prefer to maintain the original format; if the original is poor enough, prefer to use YAML...}
  ```
  </context>
