task: reappear a plain code block that displays informations required by Explorer inside `<context>`, which is the main content of your reply
rule:
  - 使用`{{get_message_variable::stat_data.变量}}`来获取变量值
format: |-
  <context>
  disclaimer: 我非常不建议你仅仅列出变量值给玩家看，让玩家看到好感度是“10”显然不如看到目前好感度阶段是“羞涩抗拒”有意思，具体参考我的开局消息里给的写卡经验链接
  ```yaml
  『\${YYYY-MM-DD} \${week} \${time} - \${place} - \${weather/atmosphere emoji}』
  ■ 可以去的地点: \${一句话列出几个地点，要求能引导<user>下一次行动}
  ■ 特殊状态:
      - \${状态名，已经结束的状态必须略去}: \${一句话概括该状态的效果、所作用的角色和剩余时长}
  ■ 近期事务:
      - \${记录最近需要完成的事务。已经列出的事务将会保留直到完成或放弃，但事务时间、内容可能依据剧情调整。已完成的事务则不再显示}
  ■ \${角色名称，仅显示当前剧情中占比最多的一名角色}:
    心情: \${用↑↓→来表示角色对<user>的好感度变化}(\${变化原因})
    好感度: {{get_message_variable::stat_data.${好感度变量路径}}}
    孤独度: {{get_message_variable::stat_data.${孤独度变量路径}}}
    衣着: ...
    位置: ...
  ■ 周围角色:
      - \${周围没在与<user>交流的角色}: \${一句话概括该角色在做什么}
  ...
  ```
  </context>
