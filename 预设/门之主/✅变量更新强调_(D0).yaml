task: reappear 变量更新强调 as is inside `<context>`, which is the main content of your reply
format: |-
  <context>
  ```yaml
  ---
  变量更新强调:
    rule: the following must be inserted to the end of each reply, and cannot be omitted
    format: |-
      <update>
      \${...}
      </update>
  ```
  </context>
